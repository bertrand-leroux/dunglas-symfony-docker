# Symfony Docker Environment Configuration

# Server Configuration
# Used by Traefik for routing (Host matching)
# For development, use app.localhost
# For production, use your actual domain name (e.g., example.com)
SERVER_NAME=app.localhost

# Mercure Public URL
# URL that clients use to connect to the Mercure hub
# For development: http://app.localhost/.well-known/mercure
# For production: https://your-domain.com/.well-known/mercure
# MERCURE_PUBLIC_URL=http://app.localhost/.well-known/mercure

# HTTP/HTTPS Ports
HTTP_PORT=80
HTTPS_PORT=443
HTTP3_PORT=443

# Symfony Configuration
# Generate with: openssl rand -hex 32
# REQUIRED in production, optional in development
APP_SECRET=

# Mercure Hub Configuration
# Generate with: openssl rand -base64 64
CADDY_MERCURE_JWT_SECRET=!ChangeThisMercureHubJWTSecretKey!

# CORS Configuration for Mercure (production)
# Comma-separated list of allowed origins
# CORS_ORIGINS=https://example.com,https://www.example.com

# Let's Encrypt Configuration (production only)
# REQUIRED for automatic HTTPS certificates in production
# ACME_EMAIL=your-email@example.com

# Database Configuration
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=!ChangeMe!
POSTGRES_VERSION=15
POSTGRES_CHARSET=utf8

# Symfony Version (for initial installation)
SYMFONY_VERSION=
STABILITY=stable

# Development Configuration
XDEBUG_MODE=off
APP_ENV=dev

# Docker Image Prefix (optional)
# Useful for custom registry
# IMAGES_PREFIX=registry.example.com/
